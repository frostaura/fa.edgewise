/**
 * /journal/review — implemented by the coach vertical. This file only re-exports so the
 * existing route keeps working; the page itself lives in features/coach/WeeklyReviewPage.
 */
export { default } from '@/features/coach/WeeklyReviewPage'
